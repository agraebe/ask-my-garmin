# Garmin Connect credentials (optional)
# When omitted, the app runs in demo mode with realistic mock data.
# Set these to connect to a real Garmin account once your app is approved.
GARMIN_EMAIL=your-garmin-email@example.com
GARMIN_PASSWORD=your-garmin-password

# Anthropic API key — get one at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...
